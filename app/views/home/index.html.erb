<h2>Hello!!!</h2>
<% if user_signed_in? %>
    <h3>Hey <%= current_user.name %></h3>
    <%= provide(:title,"Home") %>
    <h4>Click on one of the above links boss </h4>
<% else %>
        <h3>Please login to continue</h3>
    <% end %>
